repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0
    hooks:
      - id: ruff
        exclude: ^(mcpb|native|scripts)/   # fleet template scope
        args: [--fix]
      - id: ruff-format
        exclude: ^(mcpb|native|scripts)/   # fleet template scope
